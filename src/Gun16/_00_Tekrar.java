package Gun16;

import java.util.Scanner;

public class _00_Tekrar {
    public static void main(String[] args) {

    /*    Scanner oku=new Scanner(System.in);

        int []not=new int[5];
    int toplam=0;
    int ort;

        for (int i = 0; i < not.length ; i++) {
            System.out.print("NOT GIR = ");
            not[i]= oku.nextInt();
            toplam=toplam+not[i];
        }

        ort=(toplam/ (not.length));
        System.out.println("ort = " + ort);

        int ortbuyuk=0;
        int kalanlar=0;
        for (int i = 0; i < not.length ; i++) {

            if (not[i]>ort)
            {System.out.println("ortalamadan buyuk notlar="+not[i]);
                ortbuyuk++;}
            else kalanlar++;

        }
        System.out.println("ort buyuk="+ortbuyuk);
        System.out.println("kalanlar = " + kalanlar);*/


  /**  Scanner oku=new Scanner(System.in);

    int []sayi=new int[7];
int toplam=0;
int ortbuyuktek=0;
        for (int i = 0; i < sayi.length ; i++) {
            System.out.print("SAYI GIRINIZ=");
            sayi[i]= oku.nextInt();
            toplam=toplam+sayi[i];
        }
        System.out.println("toplam = " + toplam);
        int ort=(toplam/ sayi.length);
        System.out.println("ort = " + ort);
        for (int i = 0; i < sayi.length ; i++) {

            if (sayi[i]>ort && sayi[i]%2!=0)
            {System.out.println("ort buyuk ve tek sayilar = " + sayi[i]);
                    ortbuyuktek++;}
        }
        System.out.println("ortbuyuktek = " + ortbuyuktek);**/


    /* Scanner oku=new Scanner(System.in);

      String []kanallar={"trt","kanald","atv","fox","haberturk"};

        for (int i = 0; i < kanallar.length ; i++) {
            int secim=(int)(Math.random()* kanallar.length);
            System.out.println(kanallar[secim]);

        }*/











    }
}
